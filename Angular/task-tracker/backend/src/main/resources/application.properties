server.error.include-message=always
server.error.include-binding-errors=always

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.datasource.url= jdbc:postgresql://tt_db:5432/tasks
spring.datasource.username=tasktracker
spring.datasource.password=tasktracker

server.port=5200

# psql -U postgres --command="DROP DATABASE IF EXISTS tasks; CREATE DATABASE tasks;"
# psql -U postgres --command="CREATE DATABASE tasks;"

